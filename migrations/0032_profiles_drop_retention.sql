-- 버려진 축을 걷어낸다.
--
-- 궁합 시안의 "이 사람 프로필 저장하기" 를 두 브랜치가 나란히 구현했다. 한쪽은
-- 이 컬럼을 새로 만들었고, 다른 쪽은 kind 의 값 집합을 'saved'/'temp' 로 바꿨다.
-- 후자가 main 에 들어갔으므로 이 컬럼은 같은 뜻을 두 번 적어 둔 자리가 됐다.
--
-- IF EXISTS 인 이유: 이 컬럼을 만든 0016 은 그 브랜치와 함께 버려져 main 에 없다.
-- 마이그레이션을 처음부터 돌린 DB 에는 애초에 없는 컬럼이라 이 문장은 아무 일도
-- 하지 않는다. 컬럼에 걸린 CHECK 제약도 같이 떨어진다.
ALTER TABLE profiles DROP COLUMN IF EXISTS retention;
