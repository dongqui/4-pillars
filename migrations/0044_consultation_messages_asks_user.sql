-- 이 상담사 답이 사용자에게 되물어 답을 요구했는가.
--
-- 다음 턴이 "직전 답이 질문이었나"를 알아야 연속 문진을 막을 수 있는데(대화 설계 §4),
-- 그걸 말풍선 끝의 물음표로 짐작하면 양쪽으로 틀린다: "어느 쪽이 더 가까운지 말해줘요"는
-- 물음표 없이 답을 요구하고, "'내가 왜 이러지?'라는 생각이 들 수 있어요"는 물음표가
-- 있지만 되묻는 게 아니다. 그래서 답을 만든 쪽이 직접 표시하게 한다.
--
-- NOT NULL 을 걸지 않는다. 이 컬럼이 생기기 전에 쌓인 행은 알 수 없는 것이지 false 가
-- 아니다 — 그 행에는 물음표 짐작이 물러서서 답한다(prompt.ts 의 askedLastTurn).
ALTER TABLE consultation_messages ADD COLUMN IF NOT EXISTS asks_user boolean
